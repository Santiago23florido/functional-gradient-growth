"""Stable Tiny GroMo experiments.

Application layer on top of the ``fgdlib`` library: datasets
(``stable_tiny.data``), the experiment pipeline
(``stable_tiny.pipeline``), W&B logging and plotting. Everything that
trains the network lives in ``fgdlib``.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
