# Build the report. Tables in tables/ are generated by ../ablate.py; the figures
# in figures/ are produced by ../make_landscape.py. Both are regenerated from the
# repo, so `make` here only typesets.
PAPER = functional_growth

all: $(PAPER).pdf

$(PAPER).pdf: $(PAPER).tex tables/tolerance.tex tables/dataset.tex
	pdflatex -interaction=nonstopmode $(PAPER).tex
	pdflatex -interaction=nonstopmode $(PAPER).tex

clean:
	rm -f $(PAPER).aux $(PAPER).log $(PAPER).out

.PHONY: all clean
