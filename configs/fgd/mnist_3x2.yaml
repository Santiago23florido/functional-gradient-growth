run:
  name: mnist_fgd_grow_3x2
  results_dir: results
  save_plot: false
  show_plot: false

data:
  kind: mnist
  in_features: 784
  out_features: 10
  data_dir:
  batch_size: 64
  train_seed: 0
  validation_seed: 2
  test_seed: 1
  mnist_train_samples: 10000
  mnist_validation_samples: 2000
  mnist_test_samples: 2000

model:
  hidden_size: 2
  number_hidden_layers: 3
  model_seed: 0

training:
  method: fgd_approx
  epochs: 200
  accuracy_tolerance: 0.1
  gradient_clip_norm:
  log_every: 1
  device: auto

optimizer:
  name: sgd
  learning_rate: 0.01
  momentum: 0.0
  weight_decay: 0.0
  nesterov: false
  betas: [0.9, 0.999]
  eps: 1.0e-8

lr_scheduler:
  name: cosineannealing
  t_max: 50
  eta_min: 0.0
  restart_on_growth: true

fgd_approx:
  rel_error_threshold: 0.5
  rel_error_mode: tangent_projection
  layer_selection: certifying
  projection_solver: exact_kernel_eigh
  learning_rate_policy: theory_interval
  projection_damping: 0.01
  cg_max_iterations: 16
  cg_tolerance: 1.0e-4
  theory_smoothness_constant: 2.0
  theory_alpha: 1.0
  theory_beta: 1.0
  theory_mu: 2.0
  theory_loss_star: 0.0
  theory_lr_safety: 0.95
  theory_lr_initial: 0.01
  theory_lr_min: 0.0
  theory_lr_follow_bound: false
  sufficient_descent_c: 0.1
  lr_backtrack: 0.5
  lr_min_factor: 0.001
  global_bound_action: grow
  rel_error_compute_delta: true
  growth_compute_delta: false
  projection_group_size: 1
  projection_group_auto: true
  projection_group_max:
  eps: 1.0e-12
  tiny_use_covariance: true
  tiny_alpha_zero: false
  tiny_omega_zero: false
  tiny_use_projection: true
  tiny_ignore_singular_values: false
  tiny_use_fisher: false
  tiny_maximum_added_neurons:
  tiny_numerical_threshold: 1.0e-6
  tiny_statistical_threshold: 1.0e-3

scaling_line_search:
  method: golden_section
  min_value: 0.0
  max_value: 1.0
  iterations: 12
  tolerance: 0.001

growth_schedule:
  enabled: true
  every: 100
  first_epoch: 50
  layer_policy: round_robin

wandb:
  enabled: true
  project: stable-tiny
  entity:
  run_name:
  group: fgd-mnist
  job_type: train
  tags: [fgd_approx, gromo, mnist, exact_kernel_eigh, width_2]
  notes:
  mode: online
  dir: results
  log_code: false
